name: get_instance_bridge
description: "A cross-platform instance binding plugin that enables communication between native and Dart."
version: 0.1.3
homepage: https://github.com/yysy-iot
repository: https://github.com/yysy-iot/get_instance_bridge

environment:
  sdk: ^3.7.2
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  # instance_bridge_core 是纯 iOS/macOS 原生共享库，通过 CocoaPods 分发（trunk 上 0.0.17），
  # 同时发布到 pub.dev（hosted）。这里声明为插件依赖，用于 iOS/macOS 原生集成；
  # CocoaPods 模式由 podspec 的 s.dependency 'instance_bridge_core', '~> 0.0.17' 拉取。
  # SPM 模式下，插件间 path 依赖仍受 Flutter 工具链限制（见 ios/macos Package.swift 注释）。
  instance_bridge_core: ^0.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.yueying.get_instance_bridge
        pluginClass: GetInstanceBridgePlugin
      ios:
        pluginClass: GetInstanceBridgePlugin
      macos:
        pluginClass: GetInstanceBridgePlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
